<div class="row">
    <div class="twelve columns">
        <form method="post" action="/hunch/edit/{{#hunch}}{{id}}{{/hunch}}">
            <div class="row">
                <div class="six columns">
                    <h1>Create a hunce</h1>

                    <select id="select-state" name="coders[]" multiple class="demo-default" placeholder="Select some coders">
                        <option value="">Select some coders...</option>
                        {{#hunch_coders}}
                        <option value="{{id}}" selected>{{name}}</option>
                        {{/hunch_coders}}
                        {{#coders}}
                        <option value="{{id}}">{{name}}</option>
                        {{/coders}}
                    </select>

                </div>
            </div>
        </div>
        <div class="row">
            <div class="six columns">
                <textarea class="u-full-width" placeholder="What is this hunce about?" name="hunchDescription">{{#hunch}}{{description}}{{/hunch}}</textarea>
            </div>
        </div>
        <div class="row">
            <div class="six columns">
                <select id="select-tags" name="tags[]" multiple class="demo-default" placeholder="Select some tags">
                    <option value="">Select some tags...</option>
                    {{#hunch_tags}}
                    <option value="{{id}}" selected>{{tag_name}}</option>
                    {{/hunch_tags}}
                    {{#tags}}
                    <option value="{{id}}">{{tag_name}}</option>
                    {{/tags}}
                </select>
            </div>
            <div class="two columns">
                <input type="button" value="Add tag">
            </div>
        </div>
        <div class="row">
            <div class="six columns">
                <input type="radio" name="rating" value="1">1
                <input type="radio" name="rating" value="2">2
                <input type="radio" name="rating" value="3">3
                <input type="radio" name="rating" value="4">4
                <input type="radio" name="rating" value="5">5
            </div>
        </div>
        <div class="row">
            <input class="button-primary" type="submit" value="Submit">
        </div>
    </form>
</div>
</div>
<script>
    $select = $('#select-state').selectize({
        maxItems: 3,
        create: false,
    });
    $selectTags = $('#select-tags').selectize({
        maxItems: 3,
        create: false,
    });
</script>